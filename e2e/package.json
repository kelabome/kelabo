{
  "name": "@kelabo/e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-driven end-to-end tests against an offline harness of the whole stack.",
  "scripts": {
    "harness": "node harness/start.mjs",
    "check": "for f in harness/*.mjs fixtures/*.mjs tests/*.mjs playwright.config.mjs; do node --check \"$f\" || exit 1; done",
    "test": "node harness/exprTest.mjs && node harness/selftest.mjs",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
